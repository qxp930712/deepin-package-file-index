# 文件路径所属包查询概要设计说明书

| 属性 | 内容 |
|------|------|
| 项目名称 | deepin-package-file-index |
| 文档版本 | v1.0 |
| 编写日期 | 2026-08-04 |
| 概要设计 | 替代各组件独立实现文件反查包名的能力，统一为一套 D-Bus 系统服务 |

---

## 1. 背景与目标

### 1.1 现状问题

DDE 桌面多个组件需要根据文件路径反查所属 deb 包名：

| 组件 | 场景 | 当前实现 |
|------|------|----------|
| 深度安全中心 | `/proc/<pid>/exe` 识别进程所属包 | 私有查询逻辑 |
| dde-launcher | 判断应用是否由 deb 包管理 | `dpkg -S` 系统调用 |
| 窗管 dde-wm | 窗口进程归属判断 | 私有 SQLite |
| 崩溃收集器 | 崩溃进程路径定位包名和版本 | 互调 D-Bus |

存在以下问题：

- **重复造轮子**：各组件各自实现文件反查逻辑，代码重复
- **性能差**：频繁调用 `dpkg -S` 启动进程，单次调用耗时 50-200ms
- **耦合深**：组件间互调 D-Bus 形成隐式依赖
- **资源浪费**：多个组件各自解析 dpkg 数据，重复占用内存和 CPU

### 1.2 设计目标

- **统一服务**：一个 D-Bus 系统服务，所有组件通过标准接口查询
- **低延迟**：基于 mmap 只读 SQLite，查询延迟 < 1ms
- **零耦合**：消费者通过 D-Bus 调用，无需链接任何库或头文件
- **自动更新**：apt 操作后自动增量更新索引
- **纯 C 实现**：daemon 使用纯 C + libdbus，最小化依赖

---

## 2. 总体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    数据源 (离线)                            │
│  /var/lib/dpkg/status    /var/lib/dpkg/info/*.list          │
│  /usr/share/applications/*.desktop                          │
└──────────────────────┬──────────────────────────────────────┘
                       │ 解析
                       ▼
┌─────────────────────────────────────────────────────────────┐
│              deepin-pkgfile-index-build (CLI)               │
│           离线构建 SQLite 索引 (.idx 文件)                    │
│  触发方式: apt hook / postinst / 手动执行                    │
└──────────────────────┬──────────────────────────────────────┘
                       │ 写入
                       ▼
┌─────────────────────────────────────────────────────────────┐
│  /var/cache/deepin/package-file-index/installed.idx         │
│              (SQLite 只读索引文件)                            │
│                   mmap 加载                                 │
└──────────────────────┬──────────────────────────────────────┘
                       │ 读取
                       ▼
┌─────────────────────────────────────────────────────────────┐
│             deepin-pkgfile-indexd (D-Bus daemon)             │
│              纯 C + libdbus，常驻系统服务                      │
│  服务名: com.deepin.pkgfileindex                             │
│  对象路径: /com/deepin/pkgfileindex                          │
│  监听: inotify 热重载索引                                   │
└──────────────────────┬──────────────────────────────────────┘
                       │ D-Bus (system bus)
                       ▼
┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│ 安全中心  │  │ launcher │  │   窗管   │  │ 崩溃收集  │
└──────────┘  └──────────┘  └──────────┘  └──────────┘
```

---

## 3. 项目结构

```
deepin-package-file-index/
├── CMakeLists.txt
├── data/
│   ├── com.deepin.pkgfileindex.service    # D-Bus 激活配置
│   ├── com.deepin.pkgfileindex.conf       # D-Bus 策略
│   └── deepin-pkgfile-indexd.service      # systemd unit
├── src/
│   ├── lib/                               # 内部查询库 (STATIC, 不对外)
│   │   ├── CMakeLists.txt
│   │   ├── pkgfile_index.h
│   │   ├── pkgfile_index.c               # mmap + prepared statement
│   │   └── pkgfile_index_priv.h
│   ├── daemon/                            # D-Bus 守护进程 (纯 C)
│   │   ├── CMakeLists.txt
│   │   └── main.c                        # libdbus + inotify
│   └── build/                             # CLI 索引构建工具 (C++)
│       ├── CMakeLists.txt
│       ├── main.cpp
│       ├── common.h / common.cpp
│       ├── dpkg_parser.h / dpkg_parser.cpp
│       └── desktop_parser.h / desktop_parser.cpp
├── tests/
│   ├── CMakeLists.txt
│   └── test_query.cpp
├── etc/
│   └── apt/apt.conf.d/80deepin-pkgfile-index
└── debian/
```

---

## 4. D-Bus 接口设计

### 4.1 基本信息

| 项 | 值 |
|----|-----|
| 总线 | system bus |
| 服务名 | `com.deepin.pkgfileindex` |
| 对象路径 | `/com/deepin/pkgfileindex` |
| 接口名 | `com.deepin.pkgfileindex` |
| 激活方式 | D-Bus 按需激活 (D-Bus Activation) |

### 4.2 接口方法

#### 4.2.1 QueryByFile

文件精确路径 → 所属包信息。

```
方法名: QueryByFile

输入:
  file_path  s  文件绝对路径

输出:
  package_name  s  包名 (未找到返回空字符串)
  version       s  版本号
  arch          s  架构
```

**典型用途**：`/proc/<pid>/exe` → 进程所属包名。

#### 4.2.2 QueryByDesktop

Desktop 文件 → 所属包信息 + 应用名。

```
方法名: QueryByDesktop

输入:
  desktop_path  s  .desktop 文件绝对路径

输出:
  package_name  s  包名
  version       s  版本号
  arch          s  架构
  app_name      s  应用显示名称 (Name= 字段)
```

**典型用途**：启动器根据 .desktop 路径查询对应包。

#### 4.2.3 QueryPackageFiles

包名 → 该包安装的所有文件列表。

```
方法名: QueryPackageFiles

输入:
  package_name  s  包名

输出:
  files  as  文件路径数组 (未找到返回空数组)
```

**用途**：替代 `dpkg -L <包名>`。

#### 4.2.4 QueryByPrefix

路径前缀匹配。

```
方法名: QueryByPrefix

输入:
  prefix  s  文件路径前缀 (如 "/usr/lib/deepin/")

输出:
  paths         as  匹配的文件路径数组
  package_names  as  对应的包名数组
  versions       as  对应的版本号数组
```

**用途**：替代 `dpkg -S '*prefix*'`，用于模糊查询。

#### 4.2.5 GetMeta

查询索引元数据。

```
方法名: GetMeta

输入: (无)

输出:
  build_time     s   索引构建时间
  package_count  u   包总数
  file_count     u   文件总数
```

### 4.3 命令行调试

```bash
# 查询文件所属包
dbus-send --system --print-reply \
  --dest=com.deepin.pkgfileindex \
  /com/deepin/pkgfileindex \
  com.deepin.pkgfileindex.QueryByFile \
  string:/usr/bin/dde-desktop

# 查看元数据
dbus-send --system --print-reply \
  --dest=com.deepin.pkgfileindex \
  /com/deepin/pkgfileindex \
  com.deepin.pkgfileindex.GetMeta

# 查看 Introspection
dbus-send --system --print-reply \
  --dest=com.deepin.pkgfileindex \
  /com/deepin/pkgfileindex \
  org.freedesktop.DBus.Introspectable.Introspect
```

---

## 5. 索引设计

### 5.1 存储位置

```
/var/cache/deepin/package-file-index/installed.idx
```

### 5.2 SQLite Schema

```sql
PRAGMA journal_mode = WAL;
PRAGMA synchronous = FULL;
PRAGMA page_size = 4096;

-- 包信息
CREATE TABLE packages (
    pkg_id   INTEGER PRIMARY KEY,
    name     TEXT NOT NULL,
    version  TEXT NOT NULL,
    arch     TEXT,
    source   TEXT
);
CREATE INDEX idx_pkg_name ON packages(name);

-- 核心映射: 文件路径 → 包 (主查询表)
CREATE TABLE file_package (
    file_path TEXT NOT NULL PRIMARY KEY,
    pkg_id    INTEGER NOT NULL REFERENCES packages(pkg_id)
);

-- 包 → 所有文件 (替代 dpkg -L)
CREATE TABLE package_files (
    pkg_id    INTEGER NOT NULL,
    file_path TEXT NOT NULL,
    PRIMARY KEY (pkg_id, file_path)
);
CREATE INDEX idx_pkg_files_pkg ON package_files(pkg_id);

-- Desktop 文件映射
CREATE TABLE desktop_package (
    desktop_path TEXT NOT NULL PRIMARY KEY,
    pkg_id       INTEGER NOT NULL REFERENCES packages(pkg_id),
    app_name     TEXT,
    exec         TEXT
);

-- 索引元数据
CREATE TABLE meta (
    key   TEXT PRIMARY KEY,
    value TEXT NOT NULL
);
```

### 5.3 查询原理

| 操作 | SQL | 性能 |
|------|-----|------|
| QueryByFile | `SELECT ... WHERE file_path = ?` (主键) | O(log N) |
| QueryByDesktop | `SELECT ... WHERE desktop_path = ?` (主键) | O(log N) |
| QueryPackageFiles | `SELECT ... WHERE pkg_id = ?` (索引) | O(log N + K) |
| QueryByPrefix | `WHERE file_path >= ? AND file_path < ?` (范围扫描) | O(log N + K) |

Daemon 以 `SQLITE_OPEN_READONLY` + `PRAGMA mmap_size=256MB` 打开索引，多进程共享操作系统 page cache，零拷贝读取。

---

## 6. 索引构建工具设计

### 6.1 数据源

| 数据 | 路径 | 说明 |
|------|------|------|
| dpkg 状态 | `/var/lib/dpkg/status` | 所有已安装包的元信息 |
| 文件列表 | `/var/lib/dpkg/info/<name>.list` | 每个包安装的文件清单 |
| 桌面文件 | `/usr/share/applications/` (递归子目录) | .desktop 文件解析 |

> 支持多架构 .list 文件格式: `<name>:<arch>.list`

### 6.2 构建流程

```
1. 解析 /var/lib/dpkg/status → PkgInfo[]
2. 对每个包, 读取对应的 .list 文件
3. 写入临时文件 installed.idx.tmp (WAL 模式, 单事务)
     ├─ INSERT packages
     ├─ INSERT file_package
     ├─ INSERT package_files
     └─ INSERT desktop_package (匹配 .desktop → 包)
4. VACUUM 压缩, PRAGMA journal_mode=DELETE
5. fsync 确保落盘
6. rename(.tmp → .idx) 原子替换
```

### 6.3 增量更新

```
1. ATTACH 现有索引为 old_db
2. 复制 old_db 中已有的 packages/file_package/package_files/desktop_package
3. 解析当前 dpkg/status
4. INSERT OR REPLACE 更新变化的包
5. 原子替换索引文件
```

### 6.4 CLI 接口

```bash
# 全量构建
sudo deepin-pkgfile-index-build

# 增量更新
sudo deepin-pkgfile-index-build --incremental

# 指定输出路径
sudo deepin-pkgfile-index-build -o /tmp/test.idx

# 查看索引统计
deepin-pkgfile-index-build --stat
```

---

## 7. Daemon 设计

### 7.1 技术选型

| 项 | 选择 | 原因 |
|----|------|------|
| 语言 | 纯 C | 最小依赖，内存占用低 |
| D-Bus 库 | libdbus-1 | 系统自带，无需额外依赖 |
| 索引加载 | 内部静态库 | daemon 静态链接查询逻辑 |

### 7.2 启动与生命周期

```
D-Bus 激活 → 进程启动 → 连接 system bus → 请求服务名
→ 注册对象路径 → 加载索引 → 设置 inotify 监听 → 进入主循环
```

- **激活方式**: D-Bus 按需激活 (D-Bus Activation)，有查询时自动启动
- **systemd 集成**: `Type=dbus`，systemd 追踪服务状态
- **信号处理**: 捕获 SIGTERM/SIGINT 优雅退出

### 7.3 主循环

```
while (running) {
    1. dbus_connection_read_write(conn, 0)  // 非阻塞读写
    2. dbus_connection_dispatch(conn)        // 分发消息
    3. select(inotify_fd, timeout=100ms)      // 监听索引变化
       └─ 有事件 → inotify_drain() → load_index()  // 热重载
}
```

### 7.4 索引热重载

通过 inotify 监听 `/var/cache/deepin/package-file-index/` 目录：

- `IN_CLOSE_WRITE` — 索引文件被写入
- `IN_MOVED_TO` — 临时文件被 rename 为正式索引 (构建工具的原子替换)

检测到变化后重新打开索引文件，新查询立即使用新数据，无需重启服务。

### 7.5 内存管理

查询返回的字符串通过内部 StringPool 持有，在下一次查询调用后失效。Daemon 单线程顺序处理，无并发问题。

---

## 8. 更新触发机制

```
┌──────────────────┐     ┌──────────────────────┐     ┌─────────────────┐
│  apt install /   │────▶│  apt hook            │────▶│  --incremental   │
│  remove / upgrade │     │  80deepin-pkgfile-   │     │  更新索引       │
└──────────────────┘     │  index               │     └────────┬────────┘
                         └──────────────────────┘              │
                                                              ▼
                                                   ┌─────────────────┐
                                                   │  rename(.tmp→   │
                                                   │  .idx) 原子替换 │
                                                   └────────┬────────┘
                                                            │
                                                            ▼
                                                   ┌─────────────────┐
                                                   │  inotify 事件    │
                                                   │  daemon 热重载   │
                                                   └─────────────────┘
```

| 触发时机 | 机制 |
|----------|------|
| apt install/remove/upgrade 后 | `/etc/apt/apt.conf.d/80deepin-pkgfile-index` apt hook 自动增量更新 |
| 首次安装 daemon | postinst 触发全量构建 |
| 手动 | `sudo deepin-pkgfile-index-build` |

---

## 9. 部署方案

### 9.1 Debian 包

| 包名 | 内容 | 依赖 |
|------|------|------|
| `deepin-pkgfile-indexd` | daemon 二进制 + D-Bus 配置 + systemd unit | `deepin-pkgfile-index-build`, `dbus` |
| `deepin-pkgfile-index-build` | CLI 构建工具 + apt hook | `libsqlite3-0` |

### 9.2 安装文件清单

**deepin-pkgfile-indexd:**

```
/usr/bin/deepin-pkgfile-indexd
/usr/share/dbus-1/system-services/com.deepin.pkgfileindex.service
/usr/share/dbus-1/system.d/com.deepin.pkgfileindex.conf
/usr/lib/systemd/system/deepin-pkgfile-indexd.service
```

**deepin-pkgfile-index-build:**

```
/usr/bin/deepin-pkgfile-index-build
/etc/apt/apt.conf.d/80deepin-pkgfile-index
```

---

## 10. 消费者接入指南

### 10.1 Qt/C++ (推荐)

```cpp
#include <QtDBus/QtDBus>

static QString queryPkgName(const QString &filePath)
{
    QDBusInterface iface(
        "com.deepin.pkgfileindex",
        "/com/deepin/pkgfileindex",
        "com.deepin.pkgfileindex",
        QDBusConnection::systemBus());

    QDBusReply<QString> reply = iface.call("QueryByFile", filePath);
    return reply.isValid() ? reply.value() : QString();
}

static QStringList queryPkgFiles(const QString &pkgName)
{
    QDBusInterface iface(
        "com.deepin.pkgfileindex",
        "/com/deepin/pkgfileindex",
        "com.deepin.pkgfileindex",
        QDBusConnection::systemBus());

    QDBusReply<QStringList> reply = iface.call("QueryPackageFiles", pkgName);
    return reply.isValid() ? reply.value() : QStringList();
}
```

**CMakeLists.txt:**

```cmake
find_package(Qt5DBus REQUIRED)
target_link_libraries(myapp Qt5::DBus)
```

### 10.2 GLib/C

```c
#include <gio/gio.h>

GVariant *result = g_dbus_proxy_call_sync(
    proxy, "QueryByFile",
    g_variant_new("(s)", "/usr/bin/dde-desktop"),
    G_DBUS_CALL_FLAGS_NONE, -1, NULL, NULL);

const char *pkg_name;
g_variant_get_child(result, 0, "&s", &pkg_name);
```

### 10.3 命令行

```bash
dbus-send --system --print-reply \
  --dest=com.deepin.pkgfileindex \
  /com/deepin/pkgfileindex \
  com.deepin.pkgfileindex.QueryByFile \
  string:/usr/bin/dde-desktop
```

---

## 11. 性能指标

| 指标 | 目标 | 说明 |
|------|------|------|
| 单次查询延迟 | < 1ms | SQLite 主键查询 + mmap |
| 索引构建 (全量) | < 5s | 取决于安装包数量 (~3000包) |
| 索引构建 (增量) | < 1s | 仅处理变化的包 |
| 索引文件大小 | ~20-50MB | SQLite WAL 模式 + VACUUM 压缩 |
| Daemon 常驻内存 | < 10MB | 只读 mmap，page cache 由内核管理 |
| 启动时间 | < 100ms | D-Bus 激活到可服务 |

---

## 12. 依赖清单

### 12.1 构建依赖

| 依赖 | 最低版本 | 用途 |
|------|----------|------|
| cmake | >= 3.10 | 构建系统 |
| gcc | >= 8 (C11/C++14) | 编译器 |
| libsqlite3-dev | - | 索引存储 |
| libdbus-1-dev | - | D-Bus 守护进程 |
| pkg-config | - | 查找 dbus-1 |
| debhelper | >= 10 | Debian 打包 |

### 12.2 运行时依赖

| 包 | 用于 |
|----|------|
| libsqlite3-0 | build 工具运行时 |
| dbus | daemon 通信 |
